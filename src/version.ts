/**
 * Engine version — kept in sync with package.json (enforced by a unit test).
 * Games should log this in diagnostics/bug reports so issues can be traced
 * to the exact engine release:
 *
 *   console.log(`arcade-gfx ${ENGINE_VERSION}`);
 */
export const ENGINE_VERSION = '0.5.0';
